import { API_ROUTES, getCall } from 'utils/api';

export const {{ camelCase sliceName }}Api = async (payload) => {
// try {
// const { data } = await getCall(API_ROUTES.login, payload);
// return data;
// } catch (err: any) {
// throw new Error(err?.message);
// }
};